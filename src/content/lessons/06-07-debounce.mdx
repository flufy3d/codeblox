---
id: "6.7"
unit: unit-06
order: 7
title: 防止狂加分（Debounce 去抖）
emoji: "🛑"
estMinutes: 19
xp: 100
objectives:
  - 理解为什么踩一脚会一下子加好几分
  - 用一个 debounce 冷却开关，让一次踩踏只算一次
tasks:
  - id: t1
    label: 在 Pad 脚本顶部加 local debounce = false
  - id: t2
    label: 用 if player and not debounce then 包住加分
  - id: t3
    label: 加分后 task.wait(1) 再把 debounce 关掉，Play 验证
quiz:
  - q: debounce（去抖）是用来解决什么问题的？
    type: single
    options: ["踩一脚就加好几分", "方块不会变色", "玩家进不来"]
    answer: 0
    explain: debounce 像一个冷却开关，加完一分先“锁”一会儿，一次踩踏就只算一次了。
  - q: task.wait(1) 在这里的作用是？
    type: single
    options: ["等 1 秒再打开开关", "加 1 分", "删除玩家"]
    answer: 0
    explain: 加分后等 1 秒，这 1 秒内不会重复加分，过后再恢复。
  - q: not debounce 是什么意思？
    type: single
    options: ["开关没被锁住的时候", "玩家没进来", "分数是 0"]
    answer: 0
    explain: not debounce 为真，表示“现在没在冷却”，可以加分。
challenge: 把 task.wait(1) 改成 task.wait(3)，感受一下冷却变长后加分变慢。
---

## 🎯 今天要做什么

修好上节课那个“踩一脚加好几分”的毛病！我们给得分垫装一个**冷却开关（debounce）**，让**一次踩踏只算一次**，公平又好玩。

## ✨ 新知识：debounce 是一个“冷却开关”

上节课你应该发现了：一脚踩下去，`Touched` 会触发**好几次**（脚、腿、身子各算一次），于是**一次踩踏就加了好几分**。

解决办法叫 **debounce（去抖）**——其实就是一个**开关变量**：

- 一开始开关是 `false`（没锁住）。
- 加完一分，立刻把开关设成 `true`（锁住），这段时间内不再加分。
- 等 1 秒（`task.wait(1)`）后，再把开关设回 `false`（解锁）。

就像游乐场的旋转门：进去一个人，门先锁一下，过一会儿才放下一个。

这就是带 debounce 的**完整得分垫**代码：

```lua
local pad = script.Parent
local debounce = false

pad.Touched:Connect(function(hit)
	local player = game.Players:GetPlayerFromCharacter(hit.Parent)
	if player and not debounce then
		debounce = true
		player.leaderstats.Points.Value += 1
		task.wait(1)
		debounce = false
	end
end)
```

<Callout type="info">
  这个脚本放在 **Pad 这个 Part 里**。`if player and not debounce then` 表示：**是玩家**、并且**开关没锁住**，才加分。
</Callout>

<StudioStep n={1} title="加一个开关变量">
  在 Pad 脚本最上面、`pad` 那行下面，加一行 `local debounce = false`。
</StudioStep>

<StudioStep n={2} title="用开关包住加分">
  把判断改成 `if player and not debounce then`，并按上面的顺序：先 `debounce = true`，加分，`task.wait(1)`，再 `debounce = false`。

<Shot id="6.7-s1" capture="脚本编辑器里这 12 行代码（含一个空行），带行号 1-12，红框框住第 7 到 10 这四行 debounce = true / 加分 / task.wait(1) / debounce = false" alt="红框里就是那个旋转门：先上锁 → 加分 → 等一秒 → 再开锁" />
</StudioStep>

<StudioStep n={3} title="试运行">
  按 **▶️ Play**，走上 Pad——同样踩一脚，这次分数**只涨一点点**，不会再一下子跳一大截了！
</StudioStep>

## 🕹️ 看看你的成果

<Shot id="6.7-s2" capture="试玩画面右上角的计分板，跟上一课同一个位置同一块牌子，但 Points 这次只有 2，红框框住这个数字" alt="同样踩一脚 —— 上一课跳到 6，这次只有 2。旋转门管住它了 ✅" />

同样踩一脚，上一课分数直接跳到 6，现在只涨了 2——因为**1 秒之内最多只算一次**。走开再回来，又能加分。你的得分垫现在又稳又公平。

下一节就是**单元大成果**——做一个有好几块得分垫的小游戏！

<Callout type="warning">
  分数还是一脚跳好几分？检查 `debounce = true` 是不是写在加分**之前**，`debounce = false` 是不是在 `task.wait(1)` **之后**。顺序很关键。
</Callout>
