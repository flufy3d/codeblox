---
id: "4.4"
unit: unit-04
order: 4
title: "Touched：碰一下触发"
emoji: "👆"
estMinutes: 18
xp: 100
objectives:
  - 用 Touched 事件让方块“被碰到 → 在输出窗口打印一句话”
  - 试玩时走上方块，亲眼看到输出窗口里跳出文字
tasks:
  - id: t1
    label: 在 MagicFloor 这个 Part 里的 Script 中，写 local part = script.Parent
  - id: t2
    label: 写一个 onTouch 函数，里面 print("有人碰到我啦！")
  - id: t3
    label: 加上 part.Touched:Connect(onTouch)
  - id: t4
    label: 点 ▶️ 试玩，走上方块，看输出窗口跳出文字
quiz:
  - q: part.Touched:Connect(onTouch) 是什么意思？
    type: single
    options: ["方块被碰到时，就去做 onTouch 里的事", "删除方块", "给方块上色"]
    answer: 0
    explain: Connect 就是“把事件和动作连起来”：碰到（Touched）→ 执行 onTouch。
  - q: 想看 print 打印出来的文字，要打开哪个窗口？
    type: single
    options: ["输出（Output）", "资源管理器（Explorer）", "工具箱（Toolbox）"]
    answer: 0
  - q: 走上方块，输出窗口却没反应，最可能是？
    type: single
    options: ["方块勾了锚固（Anchored），所以碰不到", "Connect 那行忘了写，或函数名拼错", "Roblox 坏了"]
    answer: 1
    explain: 90% 是 Connect 没写、函数名 onTouch 拼错或大小写不对。
challenge: 把 print 里的话改成你自己的，比如 print("欢迎来到魔法地板！")。
---

## 🎯 今天要做什么

把上一课学的“事件”真正用起来！让你的 `MagicFloor` 地板**被碰到时，就在输出窗口喊一句话**。走上去试试，超有成就感！

## ✨ 新知识：把动作“连”到 Touched 上

我们要做三件事：

1. 先把方块存进盒子 `part`（第 2 课学过）。
2. 写好“碰到要做什么”——先简单地 `print` 一句话。
3. 用 `Connect` 把“碰到（Touched）”和这个动作**连起来**。

先整段抄下来，看懂结构：

```lua
local part = script.Parent

local function onTouch(hit)
	print("有人碰到我啦！")
end

part.Touched:Connect(onTouch)
```

- `local part = script.Parent`：抓住这块地板。
- `local function onTouch(hit)` … `end`：这是“碰到时要做的事”，先打印一句话。
- `part.Touched:Connect(onTouch)`：**碰到 → 就去做 onTouch**。这就是“发生某件事 → 就做某事”！

<Callout type="info">
  `hit` 是个变量，代表“碰到我的那个东西”（比如玩家的脚）。这节课先不用它，下一课会讲到。
</Callout>

<StudioStep n={1} title="打开地板里的脚本">
  在**资源管理器**里找到 `MagicFloor` 里的 **Script**，双击打开。
</StudioStep>

<StudioStep n={2} title="整段写上去">
  把原来的字删掉，照上面那段一字不差地写好（中文那句话可以换成你自己的）。
</StudioStep>

<Shot id="4.4-s1" capture="脚本编辑器里这 7 行 Touched 代码（含两个空行），带左边的行号 1-7" alt="经典的 Touched 三段式：抓住方块 → 写好要做的事 → 连起来" />

<StudioStep n={3} title="试玩走上去">
  打开**输出**窗口，点左上角的 ▶️，用 **W/A/S/D** 走到地板上踩一下。输出窗口里立刻跳出你写的那句话！🎉
</StudioStep>

<Shot id="4.4-s2" capture="视口里小人站在那块压扁的地板上" alt="走上去踩一脚 —— 这一下就触发了 Touched" />

<Shot id="4.4-s3" capture="输出面板里一行「有人碰到我啦！（x12）」，行首有个可展开的小三角，红框框住这一行" alt="一脚踩下去，这句话其实说了十几遍" />

<Callout type="info">
  看到那句话后面的 **（x12）** 了吗？意思是它被打印了 **12 遍**！你的小人有脚、腿、身体好几个部件，它们**各碰各的**，每碰一次就触发一次 `Touched`。输出窗口嫌太啰嗦，就把一模一样的话**合并成一行**、在后面记个次数——点一下行首的小三角 ▸ 就能展开看全部。想让它“碰一下只算一次”，第 6 单元会专门教一招（叫**防抖**）。
</Callout>

## 🕹️ 看看你的成果

你一踩上地板，输出窗口就喊出一句话——这是你第一次让“碰到”这件事**触发**了代码！下一课，我们把这句话换成真正的魔法：变色。✨

<Callout type="warning">
  没反应？检查两点：①最后一行 `part.Touched:Connect(onTouch)` 写了吗？②函数名 `onTouch` 两个地方拼写、大小写要**一模一样**。
</Callout>
